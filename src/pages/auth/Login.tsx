import { useEffect } from 'react';
import { Navigate, useLocation, useNavigate } from 'react-router-dom';
import { ArrowRight, LockKeyhole, ShieldCheck } from 'lucide-react';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { useAuthStore } from '../../stores/authStore';
const schema=z.object({email:z.string().email('Enter a valid email address.'),password:z.string().min(1,'Password is required.')});
type FormData=z.infer<typeof schema>;
export default function Login(){const {register,handleSubmit,setError,formState:{errors}}=useForm<FormData>();const {login,user,isLoading,error,clearError}=useAuthStore();const navigate=useNavigate();const location=useLocation();const from=(location.state as any)?.from?.pathname??'/';useEffect(()=>{clearError()},[clearError]);if(user)return <Navigate to="/" replace/>;const submit=async(v:FormData)=>{const p=schema.safeParse(v);if(!p.success){p.error.issues.forEach(i=>setError(i.path[0] as keyof FormData,{message:i.message}));return}try{await login(v.email,v.password);navigate(from,{replace:true})}catch{}};return <div className="min-h-screen bg-[#0b1220] text-white lg:grid lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden p-14 lg:flex lg:flex-col lg:justify-between xl:p-20"><div className="absolute -left-24 -top-24 h-72 w-72 rounded-full bg-emerald-400/10 blur-3xl"/><div className="relative flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-400 font-black text-slate-950">B</div><div><div className="font-semibold">Betnutri</div><div className="text-xs text-slate-400">Family Food Nutrition</div></div></div><div className="relative max-w-xl"><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[.04] px-3 py-1.5 text-xs text-slate-300"><ShieldCheck size={14}/> Controlled operations workspace</div><h1 className="text-5xl font-semibold leading-[1.08] tracking-[-.03em] xl:text-6xl">Run nutrition, food and fulfillment with confidence.</h1><p className="mt-6 max-w-lg text-base leading-7 text-slate-400">A focused workspace for the teams responsible for customers, nutrition safety, food operations, procurement, payments and delivery.</p></div><div className="relative text-xs text-slate-500">Family Food Nutrition · Internal Administration</div></section><section className="flex min-h-screen items-center justify-center bg-white px-6 py-12 text-slate-900"><div className="w-full max-w-[420px]"><div className="mb-8 lg:hidden"><div className="mb-7 flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-slate-900 font-black text-white">B</div><div><div className="font-semibold">Betnutri</div><div className="text-xs text-slate-500">Admin Portal</div></div></div></div><div className="mb-8"><h2 className="text-3xl font-semibold tracking-tight">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to your operations workspace.</p></div>{error&&<div className="mb-5 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<form onSubmit={handleSubmit(submit)} className="space-y-5" noValidate><label className="block"><span className="mb-2 block text-sm font-medium">Work email</span><input {...register('email')} type="email" autoComplete="username" className="h-12 w-full rounded-lg border border-slate-200 px-3.5 text-sm outline-none transition focus:border-slate-500 focus:ring-4 focus:ring-slate-100" placeholder="name@company.com"/>{errors.email&&<span className="mt-1.5 block text-xs text-red-600">{errors.email.message}</span>}</label><label className="block"><span className="mb-2 block text-sm font-medium">Password</span><div className="relative"><LockKeyhole className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400" size={17}/><input {...register('password')} type="password" autoComplete="current-password" className="h-12 w-full rounded-lg border border-slate-200 pl-10 pr-3.5 text-sm outline-none transition focus:border-slate-500 focus:ring-4 focus:ring-slate-100" placeholder="Enter your password"/></div>{errors.password&&<span className="mt-1.5 block text-xs text-red-600">{errors.password.message}</span>}</label><button disabled={isLoading} className="flex h-12 w-full items-center justify-center gap-2 rounded-lg bg-slate-900 text-sm font-semibold text-white transition hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-60">{isLoading?'Signing in…':<>Sign in <ArrowRight size={16}/></>}</button></form><p className="mt-8 text-center text-xs leading-5 text-slate-400">Access is controlled by Laravel authorization and your assigned administrative role.</p></div></section></div>}
